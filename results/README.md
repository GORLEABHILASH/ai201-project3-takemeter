# results/

Artifacts downloaded from the Colab notebook after each evaluation run. Drop these here and commit them:

- `evaluation_results.json` — metrics from the latest run (accuracy, per-class precision/recall/F1, etc.)
- `confusion_matrix.png` — confusion matrix visualization for the latest run

These are regenerated each Colab run — overwrite and re-commit to keep the repo current.
